# 2026-06-03 Release

Release version: `2026.6.3`

This release publishes the current audited `kicad-monkey` package for downstream
KiCad SVG and design-review tooling on 2026-06-03.

## Notes

- Carries forward the enriched PCB/SVG metadata, schematic SVG metadata, and
  schematic instance API introduced in `2026.6.2`.
- Intended as the `kicad-monkey` release for downstream packages that need a
  stable public dependency pin on 2026-06-03.

